# Secrets are managed via Infisical (https://infisical.com).
# Run `infisical run --env=dev -- npm run dev` to inject secrets locally.
#
# Required secrets (set in Infisical for each environment):
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
